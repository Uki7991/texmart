<a href="{{ $route ?? '#' }}" class="btn {{ isset($bootstrap3) && $bootstrap3 ? 'btn-primary' : '' }} border border-dark shadow-sm rounded-pill mx-1 call-btn transition-100 {{ $class ?? '' }}">
    Связаться <i class="fas fa-phone text-danger"></i>
</a>
